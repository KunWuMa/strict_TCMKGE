# PairComp-KGE experiments

Run scripts from the repository root. The environment is defined in `environment.yml`, source data placement is described in `../../01_data/README.md`, and the main protocol is in `EXPERT_SYSTEMS_EXPERIMENT_PROTOCOL.md`. The `results/` and `figures/paper/` directories contain aggregate outputs. Manuscript and record-level artifacts are not distributed.
